# Note

These notebooks lived outside this folder during development. The notebooks are attached here mostly for reference, and are not intended to be used as is. If required, a one-liner: `import sys; sys.path.append("..")` may be required to run certain parts of this code, and model loading filepaths may need to be changed. Or, simply copy/move it outside and it should be good.