# Note

The scripts in this directory are attached here mostly for reference, and were used during development. They are not intended to be used. If required, they need to be called from the project main directory as `bash scripts/ablations.sh`, etc.